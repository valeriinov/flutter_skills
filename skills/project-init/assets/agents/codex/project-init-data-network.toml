name = "project_init_data_network"
description = "Creates data result types, base repositories, error handling, and optional Dio network/env scaffolding."
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Own data and optional network foundation for project-init.

Write scope:
- lib/domain/entities/data_result/
- lib/data/repositories/base_repository.dart
- lib/data/utils/error_handling/
- lib/data/network/
- network-related DI modules

Do not edit outside your write scope. Other agents may be working in parallel.
Create DataResult and failure types backed by dartz. If useDio is true, add Dio,
env provider, error handling, constants, and DI modules. If useDio is false, do
not leave dangling Dio imports. Do not add backend-specific SDKs or local
database scaffolding.
"""
