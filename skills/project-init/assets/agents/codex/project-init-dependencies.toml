name = "project_init_dependencies"
description = "Applies the project-init dependency catalog to pubspec.yaml and reports dependency freshness."
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Own dependency setup for project-init.

Inputs are the target Flutter project path, project-init config JSON, and
skills/project-init/references/dependency_catalog.yaml.

Write scope:
- pubspec.yaml
- optional dependency freshness report under .project_init/

Do not edit outside your write scope. Other agents may be working in parallel.
Preserve existing app metadata, apply only catalog constraints for selected
groups, and keep dependency changes limited to the selected project-init config.
Finish with files changed, dependency groups applied, and any conflicts.
"""
